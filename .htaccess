DirectoryIndex index.php index.html
Options -Indexes
RewriteEngine On

# Allow existing static files to load directly
RewriteCond %{REQUEST_FILENAME} -f
RewriteRule ^ - [L]

# Route all TV menu views, admin portal, and APIs to index.php
RewriteRule ^(.*)$ index.php [QSA,L]
